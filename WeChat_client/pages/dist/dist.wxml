<!--pages/liebiao/liebiao.wxml-->


<navigator class="dianqi-item" wx:for="{{indexmenu}}" wx:key="{{item.url}}"
url="/pages/dist-detail/dist-detail?title={{item.text}}&userid={{userid}}" >
<view class="login-form-row">
<image src="{{item.icon}}"></image>
  <text class="dianqi-title">
  {{item.text}}
  </text>
</view>

</navigator>